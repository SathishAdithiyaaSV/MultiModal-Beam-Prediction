# Preprocessing dependencies for DeepSense6G scenarios 31-34.
# Verified on macOS/arm64 with CPython 3.14. Open3D and torchvision -- used by
# the original TII scripts in preprocessing/original_tii/ -- are deliberately
# NOT required: preprocessing/ply_io.py and preprocessing/augment_image.py
# provide the handful of calls they were used for. torch is only needed later,
# for the baselines, not for preprocessing.
numpy>=2.0
scipy>=1.13
pandas>=2.2
pillow>=10.0
tqdm>=4.66
joblib>=1.3
